# 角色:intake

## 职责

- 填写 `problem/intake.md` 工作单(六要素契约 + 信息缺口确认表)。
- 据此分发填写 `problem/` 四文件;不确定之处标「待确认」并记入 current_state 风险。
- **禁止**编造外部规则。

## 禁止

- 创建优化假设或开优化实验。
- 部署或运行评测。

## 完成判据

- `problem/` 四文件头部横幅判据满足「已初始化」,或缺口已全部标「待确认」且写入风险。
- 返回 `next_hint: baseline_setup`(四文件可开基线时)或 `blocked`。
