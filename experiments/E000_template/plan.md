# 实验计划

> 模板文件:复制到新实验目录后填写,并删除本横幅。实验执行前必须完成本文件;
> 信息不足时显式写下假定值并标"待确认",不要留空。

## 实验编号
Exxx

## 实验名称
填写实验名称。

## 元数据
- 创建日期:YYYY-MM-DD
- 执行者:(人名或 Agent 会话标识)

## 关联假设
Hxxx(基线或探索性实验填 none)

## 对比基线
Exxx(说明本实验与哪个实验对比。本实验即基线时填"无——本实验为基线",并填写文末"基线契约"一节)

## 实验目的
说明本实验要验证什么。

## 先验检索
动手前检索 references/(领域判读规则与排查决策树)、memory/(insight_bank、decision_log、gotchas、evidence_index、component_index)与既往 Exxx,逐条记录;无命中填"无相关条目"。

| 条目 | 内容摘要 | 对本实验设计的影响 |
| --- | --- | --- |
| (Ixxx / Dxxx / EVDxxx / Exxx) |  |  |

记忆是线索不是证据:引用的旧结论不能替代本实验的测量。

## 实验变量
说明本次实验改变了什么(一次只改少量变量)。

## 固定条件
说明哪些参数、环境、数据、接口或流程保持不变。

## 关注指标与测量方法
- 指标及统计口径:(p50/p99/均值等,与 problem/scoring-and-sla.md 保持一致)
- 重复次数:N 次(改进幅度接近噪声阈值时必须增加重复;单次测量不得登记为证据)
- 统计方法:(多次测量如何聚合:均值±方差 / 中位数等)

## 成功标准
说明什么结果算成功,引用可测指标与阈值。

## 失败标准
说明什么结果算失败或无效。

## 中止与作废条件
- 超时:运行超过 ___ 仍未产出结果即中止,实验标 failed。
- 环境信号:出现哪些信号(如资源耗尽、服务崩溃、数据源异常)立即停止并标 invalid,不再继续分析。
- 干扰:测前/测中发现共享环境有其它任务干扰测量时先等待;无法排除则停止并标 invalid,禁止硬跑。

## 风险
说明本实验可能带来的风险。

## 预期结果
说明实验前的预期。

## 理论上界估算(可选)
估算本优化方向的理论极限(带宽/算力/排队模型/Amdahl 上界)与当前实测的差距,
作为"还剩多少空间、值不值得继续"的判断依据;report/ 中声称"已接近理论极限"时必须引用本节。
- 上界模型与假设:
- 理论上界值:
- 与当前实测的差距:

## 范围边界(可选)
- 允许的改动范围:
- 禁止使用的手段:

## 基线契约(仅基线实验必填,其余实验删除本节)
- 环境指纹:(硬件、系统、依赖版本、目标系统 commit,或环境快照文件路径)
- 固定负载定义:(数据集 / 请求构成 / 并发 / 时长)
- 指标统计口径:(与 problem/scoring-and-sla.md 一致)
- 可复制的复测命令:
- 冻结日期:YYYY-MM-DD

冻结后不得修改基线;确需更换基线时,必须新开实验编号建立新基线,并在 memory/decision_log.md 记录原因,此后所有实验的对比一律指向新基线编号。
